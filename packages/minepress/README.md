# minepress

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build minepress` to build the library.

## Running unit tests

Run `nx test minepress` to execute the unit tests via [Jest](https://jestjs.io).
