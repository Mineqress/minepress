# minexpect

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build minexpect` to build the library.

## Running unit tests

Run `nx test minexpect` to execute the unit tests via [Jest](https://jestjs.io).
